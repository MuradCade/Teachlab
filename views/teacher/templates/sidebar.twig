   <div class="sidebar" id="sidebar" style='overflow:auto;'>
        <div class="p-3">
            <h4 class="navbar-brand border-bottom">
                <img src="/assets/img/logo.png" alt="teachlab logo" width='140' >
            </h4>
        </div>
        
        <nav class="nav flex-column gap-0">
            <a class="nav-link font-md border-bottom {{current_page == 'dashboard.home'?'active':''}}" href="{{url_for('teacher.dashboard.home')}}">
                <i class="fas fa-th-large me-2"></i> Dashboard
            </a>
            <a class="nav-link font-md border-bottom {{current_page == 'dashboard.course'?'active':''}}" href="{{url_for('teacher.course.index')}}">
                <i class="fas fa-book me-2"></i> Courses
            </a>
            <a class="nav-link font-md border-bottom {{current_page == 'dashboard.student'?'active':''}}" href="{{url_for('teacher.student.index')}}">
                <i class="fas fa-users me-2"></i> Students
            </a>
            <a class="nav-link font-md border-bottom {{current_page == 'dashboard.attendance'?'active':''}}" href="{{url_for('teacher.attendance.index')}}">
                <i class="fas fa-calendar-check me-2"></i> Attendance
            </a>
            <a class="nav-link font-md border-bottom" href="#">
                <i class="fas fa-tasks me-2"></i> Assignments
            </a>
            <a class="nav-link font-md border-bottom" href="#">
                <i class="fas fa-question-circle me-2"></i> Quizzes
            </a>
            <a class="nav-link font-md border-bottom" href="#">
                <i class="fas fa-file-alt me-2"></i> Exams
            </a>
            <a class="nav-link font-md border-bottom" href="#">
                <i class="fas fa-table me-2"></i>  
                Overall Performance
            </a>
        </nav>
        
        <!-- Upgrade Card -->
        <div class="upgrade-card p-2  mt-1 small" style='position:relative; right:0px;'>
            <div class="d-flex align-items-center mb-1">
                <i class="fas fa-crown me-2"></i>
                <strong>Upgrade to Pro</strong>
            </div>
            <p class="small mb-2">Gain Full Access to Advanced Features & Analytics</p>
            <button class="btn btn-light btn-sm w-100 fw-bold">Upgrade Now</button>
        </div>
    </div>